{
  "_comment": "Starter deny rules encoding the charter's hard limits. VERIFY pattern syntax against current Claude Code permissions docs before relying on these — intent per line: no merging, no force-push, no applies/destroys outside CI, no workflow/permissions edits (devops exception is granted per-session, not here), no branch deletion.",
  "permissions": {
    "deny": [
      "Bash(gh pr merge:*)",
      "Bash(git push --force:*)",
      "Bash(git push -f:*)",
      "Bash(git push origin --delete:*)",
      "Bash(terraform apply:*)",
      "Bash(terraform destroy:*)",
      "Bash(gcloud * delete *)",
      "Edit(.github/workflows/**)",
      "Write(.github/workflows/**)",
      "Edit(.claude/settings.json)",
      "Write(.claude/settings.json)"
    ]
  }
}
